/**
 * Game class stub for TypeScript compatibility
 * Main implementation is in src/game.js
 */

export class Game {
  private canvas: HTMLCanvasElement | null = null
  
  constructor() {
    console.log('🎮 Game class stub - redirecting to src/game.js implementation')
  }
  
  async init(): Promise<void> {
    console.log('🔄 Game.init() - Use DragonHunterGame from src/game.js instead')
  }
  
  start(): void {
    console.log('🚀 Game.start() - Use DragonHunterGame from src/game.js instead')
  }
}
